<ul>
    {{#each posts}}
    <li>
        <h2><a href="/post/{{id}}">{{title}}</a></h2>
        <h3>Created {{createdAt}} by {{user.username}}</h3>
        <p>{{post_text}}</p>
    </li>
    {{/each}}
</ul>